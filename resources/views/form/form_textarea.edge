@if(label)
<label for="{{name}}" class="form-label {{required ? 'required' : ''}}">{{label}}</label>
@endif
@!component('form/form_rule', {rule})
<textarea
  class="form-textarea"
  minlength="{{minlength ? minlength : ''}}"
  maxlength="{{maxlength ? maxlength : ''}}" 
  {{--  {{required ? 'required' : ''}}    --}}
  name="{{name}}" id="form-textarea-{{name}}">{{flashMessages.has(name) ? flashMessages.get(name) : ''}}
</textarea>
@!component('form/form_error', {name})
