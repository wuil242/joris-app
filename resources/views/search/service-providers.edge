@each(sp in serviceProviders)
  @!component('service_provider/parts/service-provider-card', {sp})
  @endeach
@if(serviceProviders.hasMorePages)
  <button class="button search-more-button" type="submit" id="more-button" data-page="{{serviceProviders.currentPage + 1}}">More</button>
@endif
