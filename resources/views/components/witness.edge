@each(witness in witnesses)
<div class="witness">
  <img class="witness-image" src="#" alt="witness image">
  <p class="witness-text">{{witness.text}}</p>
  <p class="witness-name">{{witness.name}}</p>
</div>
@endeach